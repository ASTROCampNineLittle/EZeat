<div class='w-full max-w-sm'>
  <div class="md:flex md:items-center mb-6">
    <div class=" ">
        <h1 class='block text-black-500 text-3xl font-bold md:text-right mb-1 md:mb-0 pr-4'>編輯方案</h1>
    </div>
  </div>

  <%= form_with model: [:backend, @dish], class: " " do |form| %>
    <div class="md:flex md:items-center mb-6">
      <div class="md:w-1/3">
        <%= form.label :name, '方案名稱', class: 'block text-gray-500 font-bold md:text-right mb-1 md:mb-0 pr-4' %>
      </div>
      <div class="md:w-5/6">
        <%= form.text_field :name, class: 'bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500' %>
      </div>
    </div>

    <div class="md:flex md:items-center mb-3">
      <div class="md:w-1/6">
        <%= form.label :food_type, '方案價格', class: 'block text-gray-500 font-bold md:text-right mb-1 md:mb-0 pr-4' %>
      </div>
      <div class="md:w-5/6">
        <%= form.text_field :price, class: 'bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500' %>
      </div>
    </div>

    <div class="md:flex md:items-center mb-3">
      <div class="md:w-1/6">
        <%= form.label :intro, '方案介紹', class: 'block text-gray-500 font-bold md:text-right mb-1 md:mb-0 pr-4' %>
      </div>
      <div class="md:w-5/6">
        <%= form.text_area :intro, class: 'bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500' %>
      </div>
    </div>

    <div class="md:flex md:items-center mb-6">
      <div class="md:w-1/3">
        <%= form.label :address, '開放狀態', class: 'block text-gray-500 font-bold md:text-right mb-1 md:mb-0 pr-4' %>
      </div>
      <div class="md:w-5/6">
        <%= form.select(:status, [['未開放', false], ['開放中', true]], {},  { :class => 'bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700 leading-tight focus:outline-none focus:bg-white focus:border-purple-500'})  %>
      </div>
    </div>

    <div class="md:flex md:items-center mb-6">
      <% @dish.images.each do |dish| %>
       <%= image_tag dish.image_url if dish.image_url.present? %>
      <% end %>  
      <%= form.fields_for :images do |image| %>
        <div class="form-group">
          <%= image.label :image, "更新圖片" %>
          <%= image.file_field :image, class: "" %>
        </div>
      <% end %>
    </div>

    <div class="md:flex md:items-center justify-end">
      <div class=" "></div>
      <div class=" ">
        <%= form.submit value="更新資料", class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline' %>

        <%= link_to backend_store_dishes_path(@dish.store), class: 'bg-gray-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline' do %>
          取消
        <% end %>
      </div>
    </div>
  <% end %>
</div>

